<h1><%= @user.name %> Chat room</h1>

<form>
    <label>
        message: <input type="text" data-behavior="room_speaker">
    </label>
</form>
<br/>
<div id="messages">
    <% @messages.each do |message| %>
        <div><%= "#{message.user_name}: #{message.content}" %></div>
    <% end %>
</div>